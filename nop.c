#include "monty.h"
/**
 * f_nop- doesn't do anything
 * @head: stack head
 * @counter: line_number
 * Return: return nothing
*/
void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
